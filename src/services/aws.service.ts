import UploadsModel from '../models/uploads/uploads.model';
import {UploadFileRequestData} from '../types';

class AwsService {
	public uploadFile = async (fileData: UploadFileRequestData): Promise<void> => {

	}
}

export default new AwsService();
